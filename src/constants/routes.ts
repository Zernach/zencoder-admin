export enum STACKS {
  DASHBOARD = "(dashboard)",
}

export enum ROUTES {
  ROOT = "/",
  DASHBOARD = "/dashboard",
  AGENTS = "/agents",
  COSTS = "/costs",
  GOVERNANCE = "/governance",
  SETTINGS = "/settings",
}
