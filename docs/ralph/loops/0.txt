Read the `docs/pull_requests/0000-task-manager.md` file and the current directory structure of the React Native codebase, then decide which is the first incomplete pull_request, read its associated markdown plan, and begin working on it.

After you complete a pull_request in its entirety, you may commit, but do NOT push to the remote repository. I want to review one commit for each pull_request.

Only output `DONE` after all pull_requests in the `0000-task-manager.md` file are ✅
